---
import { t } from '../../lib/i18n';
const { block, lang } = Astro.props;
---
<figure class="b-quote">
  <blockquote>{t(block.text, lang)}</blockquote>
  {block.cite && <figcaption>— {t(block.cite, lang)}</figcaption>}
</figure>
